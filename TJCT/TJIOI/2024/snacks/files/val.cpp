#include <bits/stdc++.h>
#include "testlib.h"
 
using namespace std;
 
int main(int argc, char *argv[]) {
    registerValidation(argc, argv);
 
    int x = inf.readInt(1, 1'000'000'000, "x");
    inf.readSpace();
    int y = inf.readInt(1, 1'000'000'000, "y");
    inf.readSpace();
    int z = inf.readInt(1, 1'000'000'000, "z");
    inf.readEoln();
    inf.readEof();
    
}
